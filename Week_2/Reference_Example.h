#ifndef REFERENCES_H
#define REFERENCES_H

#include "iostream"

// Let's write some functions and compare references and pointers
// Most C++ code you will see in this course will be contained
// in class contexts but here we just want to look at some syntax

void swapPointers(char*, char*);

void swapReferences(char&, char&);

#endif